# recall

`recall` is a CLI study partner that retrieves chunks from your codebase and forces you to explain them before revealing the answer. It keeps receipts on your misconceptions. It does not learn from you in the ML sense. It is a persistent journal with a retrieval interface.
